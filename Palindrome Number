#include <stdio.h>

int main()
{
    int reminder,rev=0,N;
    printf("Number:\n");
    scanf("%d",&N);
    
    do
    {
       reminder=N%10;
       rev=(rev*10)+reminder;
       N=N/10;
    } while(N>0);
    
    printf("%d is Palindrome Number",rev);

    return 0;
}
