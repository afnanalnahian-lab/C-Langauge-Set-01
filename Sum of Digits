#include<stdio.h>
int main() 
{
    int N,sum=0,dig;
    printf("Enter Your Number:\n");
    scanf("%d",&N);
    
    while(N!=0)
    {
        dig=N%10;
        sum=sum+dig;
        N=N/10;
    }
    printf("Sum is %d",sum);
    return 0;
}
 
