#include <stdio.h>
int main()

{
    int N,rev=0,dig;
    printf("Enter Your Number:\n");
    scanf("%d",&N);
    
    while(N!=0)
    {
        dig=N%10;
        rev=rev*10+dig;
        N=N/10;
    }
    printf("Reverse Number:%d\n",rev);

    return 0;
}
