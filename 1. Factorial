#include <stdio.h>

int main()
{
    int i=1,fac=1,N;
    printf("Enter Factorial Number:\n");
    scanf("%d",&N);
    
    do
    {
       fac=fac*i;
       i=i+1;
       
    } while(i<=N);
    
    printf("Factorial is %d:\n",fac);
    
    return 0;
}
