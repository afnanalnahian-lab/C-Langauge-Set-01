#include <stdio.h>

int main()
{
    int first=0,second=1,next,i,N;
    printf("Number:\n");
    scanf("%d",&N);
    
    for(i=1;i<=N;i++)
    {
        printf("%d ", first);
        
        next=first+second;
        first=second;
        second=next;
    }

    return 0;
}
