#include <stdio.h>

int main()
{
    int i,a,fac=1;
    printf("Enter Your Factorial:\n");
    scanf("%d",&a);
    
    for(i=1;i<=a;i++)
    {
        fac=fac*i;
    }
    
    printf("Factorial is %d",fac);

    return 0;
}
